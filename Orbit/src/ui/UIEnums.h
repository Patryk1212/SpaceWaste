#pragma once

enum class UIType
{
	NONE = 0,
	STANDARD,
	SLIDER,
	BUTTON_LIST,
	OBJECT_LIST
};

enum class UIWindowInstance
{
	NONE = 0,
	EXIT,
	GENERAL_INFO,
	OBJECTS_LIST,
	CONTROLS,
	TIME_LINE
};

