#pragma once

/* --- Space Object Size --- */
static const float MIN_SIZE_LOW = 2.f;
static const float MAX_SIZE_LOW = 6.f;

static const float MIN_SIZE_MIDDLE = 6.f;
static const float MAX_SIZE_MIDDLE = 11.f;

static const float MIN_SIZE_HIGH = 11.f;
static const float MAX_SIZE_HIGH = 14.f;

static const float SIZE_THE_FURTHEST = 24.f;

/* --- Distance To Earth --- */
static float CLOSE = 1000.f;
static float MID = 3700.f;
static float FAR = 4500.f;